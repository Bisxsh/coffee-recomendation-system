name: Weekly TikTok Specials Scraper

on:
  schedule:
    - cron: "0 23 * * 0"
  workflow_dispatch:

jobs:
  run-weekly:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository data layer
        uses: actions/checkout@v4

      - name: Setup runtime engine environment
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"
          cache: "pip"

      - name: Install dependencies
        run: pip install requests

      - name: Execute Scraper Daemon
        env:
          TIKTOK_SCRAPER_API_KEY: ${{ secrets.TIKTOK_SCRAPER_API_KEY }}
          TIKTOK_SCRAPER_API_URL: ${{ secrets.TIKTOK_SCRAPER_API_URL }}
          TIKTOK_SCRAPER_API_HOST: ${{ secrets.TIKTOK_SCRAPER_API_HOST }}
        run: python get_tiktok_data.py --mode weekly

      - name: Commit and flush updated weekly snapshot
        run: |
          git config --local user.email "action@github.com"
          git config --local user.name "GitHub Action Bot"
          git add public/tiktok_week.json
          git diff --quiet && git diff --staged --quiet || (git commit -m "data: automated weekly specials sweep" && git push)
